# vision-to-language-captioning
An end-to-end image captioning system using deep learning, combining multiple CNN architectures (VGG16, ResNet50, EfficientNetB0, InceptionV3) with sequence models (LSTM and Transformers) to generate accurate and meaningful image descriptions, along with a GUI and custom dataset built via web scraping.
